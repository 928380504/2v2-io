# Template Changelog

本文件记录模板中有意义的代码、配置、结构和文档变化。当前行为以 `TEMPLATE-GUIDE.md` 为准。

## [Unreleased]

- 暂无。

## [2.26.2] - 2026-08-22

### Fixed

- Hot Games 页面改为统一使用模板核心的全站热度前 21 名，不再读取站点迁移时固化的 7、8、12 等数量上限。
- 旧站提取器不再使用 `Math.min(21, extractedGames.length)` 把迁移当时的游戏总数写成永久上限；新增游戏会自动参与统一热度排序，并在进入前 21 名后显示。

### Changed

- 站点蓝图、站点生成器、反向导出器、示例蓝图和本地可视化后台移除 `hotGames.limit`，避免不同网站再次产生不一致的 Hot Games 上限；旧蓝图中的该字段会被安全忽略。

### Verification

- 增加旧版 `hotGames.limit` 兼容清理、生成源码无单站上限、旧站提取无固化上限以及核心固定 21 名契约的回归检查。

## [2.26.1] - 2026-08-20

### Added

- 新增数据库结构与消耗优化权威规范，统一说明评论、评分、播放、点赞、踩、收藏等社区基础数据，以及事件、跑马灯、聚合统计、排名快照、个人窗口、主动刷新和奖牌等竞赛扩展数据。
- 规范包含真实表职责、索引设计、缓存分层、快照原子切换、跨游戏适配边界、新站建库流程、迁移规则、反模式、消耗指标和发布验收清单。

### Changed

- `AGENTS.md` 将数据库优化规范设为所有 D1、迁移、社区互动、缓存与竞赛数据修改的强制必读文档，防止后续 AI 绕过聚合表、快照、共享缓存和索引结构。
- 模板总指南增加数据库规范入口，并明确数据库规范与跑马灯/排行榜契约需要同时遵守。

### Verification

- 对照现有 10 个迁移、社区后端、1v1 竞赛适配器、缓存配置和 Cloudflare 部署流程逐项核实文档；覆盖文档一致性、后端校验、TypeScript、Pages Functions 和完整静态构建。

## [2.26.0] - 2026-08-20

### Added

- 公共日榜与总榜接入 Cloudflare `caches.default`，以规范化的周期、日期、页数和偏移量作为共享缓存键；个人排名窗口与主动实时刷新继续保持独立、不可共享。
- 通用 HTTP 核心新增共享 GET 缓存键、读取和异步写入工具；本地或不提供 Cache API 的环境会安全回退到直接响应。

### Changed

- 快照榜单把当前排名范围与最后一名改写为两个 `UNION ALL` 索引分支，移除导致整个快照被扫描的 `OR` 条件。
- 日榜缓存键显式包含站点日期，避免纽约时区跨日时复用昨日响应；总榜边缘缓存继续在站点日重置前自动缩短有效期。

### Verification

- 覆盖 TypeScript、后端契约、Cloudflare Pages Functions Worker 编译、模板发布校验与完整静态构建。

## [2.25.0] - 2026-08-20

### Added

- 新增公共游戏页面上下文；首页主游戏和对应游戏内页现在统一读取同一条游戏记录、分类、游玩地址和相关推荐。
- 首页复用内页的 `VideoGame + WebApplication + AggregateRating` 结构化数据组件，并通过页面 URL 覆盖生成根页面实体。
- 完整构建新增首页—内页结构化数据契约检查，防止页面 URL、主实体关联或聚合评分在后续模板升级中回退。

### Changed

- 首页继续保留独立的封面文案、排行榜、侧栏、视频、FAQ 和正文组合，但不再单独解析或拼装另一份主游戏数据。
- 首页 `WebPage` 结构化数据通过 `mainEntity` 关联主游戏，结构化评分与可见评分继续共享相同游戏 ID、构建期快照和客户端缓存。

### Verification

- 覆盖首页与游戏内页的公共上下文、页面专属结构化 URL、首页聚合评分静态输出、TypeScript 与完整静态构建。

## [2.24.0] - 2026-08-20

### Changed

- 全站热门排序统一为 `播放量 × 1 + 点赞 × 5 + 收藏 × 50`，热门页、分类页、筛选页和相关游戏榜不再各自按局部播放量重新排序。
- 新增全局游戏排名快照；真实卡片统计完整时统一使用实时数据，数据不完整时整批回退到目录初始值，避免静态值与局部实时值混排。
- 方形游戏卡片角标改为按全站规模自适应：少于 30 个游戏使用 TOP 1–3、HOT 4–8、NEW 最近 5 个，达到 30 个后使用 TOP 1–5、HOT 6–15、NEW 最近 10 个；NEW 限定 45 天，角标优先级为 `TOP > NEW > HOT`。
- 游戏卡片统计请求支持超过 50 个游戏时自动拆批，模板 100 游戏上限内仍可生成完整全站排名。

### Verification

- 覆盖全局热度排序、全局名次复用、自适应角标、分类与筛选后名次不重排，以及多批卡片统计的 TypeScript、站点生成和静态构建验证。

## [2.23.8] - 2026-08-19

### Fixed

- `site:create -- --apply` 现在会在路由类型生成前清理 `.next`、`out/types` 和 `tsconfig.tsbuildinfo`，彻底隔离 Next.js 13 升级到 16 后遗留的路由清单、类型文件与增量缓存。
- Next.js 路由类型生成或 TypeScript 校验失败时直接附带具体输出，不再只返回泛化错误。

### Verification

- 在保留旧 `BQ/TK/YS` 与独立游戏路由缓存的 BuildNow GG 站点复现失败；清理三类生成缓存后，蓝图应用、路由类型生成和 TypeScript 校验一次通过。

## [2.23.7] - 2026-08-19

### Fixed

- `site:create -- --apply` 在 TypeScript 校验前先执行 Next.js 路由类型生成，避免 `.next/types` 中重构前的旧路由造成伪失败。
- Next.js 与 TypeScript 缺失或执行失败时分别返回明确错误，便于定位依赖和类型问题。

### Verification

- 使用存在旧路由类型缓存的 `1v1-lol` 站点复现问题；刷新路由类型后，同一蓝图应用与 TypeScript 校验均通过。

## [2.23.6] - 2026-08-19

### Fixed

- 模板同步器改为真正的三方判断：模板未改但站点已改的核心文件现在归入 `Preserved local`，不再误报冲突或阻塞其他更新。
- 只有模板与站点同时修改同一文件时才报告 `Local conflicts`，同步汇总新增保留本地文件数量。
- 新增本地单边修改、模板单边修改和双方并发修改的分类测试。

### Verification

- 覆盖本地定制保留、干净目标自动更新和真实并发冲突三种同步路径。

## [2.23.5] - 2026-08-19

### Fixed

- 模板发布与同步哈希对常见文本文件统一使用 LF，Windows Git 的 CRLF 检出不再被误判为本地核心冲突。
- 二进制文件继续按原始字节计算 SHA-256，避免换行兼容逻辑掩盖真实资源变化。
- 新增文本换行等价和二进制字节敏感两组自动化测试。

### Verification

- 覆盖 LF/CRLF TypeScript 文件哈希一致性，以及二进制文件原始字节差异检测。

## [2.23.4] - 2026-08-19

### Fixed

- 页脚每列的标题与链接合并为整体居中的左对齐内容块，使栏目标题和链接共享同一条左侧基线。

### Verification

- 覆盖 Games、Company、Legal 三列和移动端单列的标题—链接对齐关系。

## [2.23.3] - 2026-08-19

### Changed

- 页脚三列标题继续居中，链接列表改为整体居中、内部文字左对齐，使每列链接起点整齐。

### Verification

- 覆盖 Games、Company、Legal 三组不同长度链接以及移动端单列布局。

## [2.23.2] - 2026-08-19

### Fixed

- 页脚 Games、Company、Legal 三列改为等宽居中分布，列内标题和链接统一居中，并移除 Games 链接独有的水平内边距。

### Verification

- 覆盖移动端单列与桌面三列页脚的视觉中心和免责声明对齐。

## [2.23.1] - 2026-08-19

### Fixed

- 导航栏恢复全宽内层布局，不再跟随页面主体的统一容器。
- 主页脚恢复 Tailwind 独立 `container`，友情链接恢复 1200px 独立上限；主体统一宽度只作用于实际页面内容。

### Verification

- 覆盖导航栏、主页脚、友情链接与页面主体容器之间的宽度隔离。

## [2.23.0] - 2026-08-19

### Changed

- 新增全站统一的 `.site-container-width` 容器规则；导航栏内层、跑马灯、首页、游戏内页、分类页、热门页、标签页、法律页、友情链接和页脚现在共享同一组宽度限制。
- 桌面统一采用 `clamp(960px, 70vw, 1574px)` 与 16px 视口安全边距，站点继续通过既有三个游戏宽度字段统一覆盖全站容器。
- 可视化后台将原游戏宽度字段改名为全站容器宽度，隐藏已经不再影响布局的旧列表宽度字段；旧 CSS 类和变量继续作为兼容别名保留。

### Verification

- 覆盖首页、游戏内页、分类页、热门页、标签筛选页、五个法律页、导航栏、跑马灯、友情链接及页脚的统一左右边界。

## [2.22.11] - 2026-08-19

### Changed

- `Top games` 与 `Top players` 标题字号由 11px 放大为原 `Arena Legends` 使用的 16px，彩色火焰和奖杯同步按比例放大为 20px。

### Verification

- 保持单行标题、明暗主题文字颜色及右侧操作按钮布局不变。

## [2.22.10] - 2026-08-19

### Changed

- `Top players` 的单色线框奖杯改为与游戏榜一致的 15px 彩色奖杯，并将标题同步放大至 11px。
- `Top games` 与 `Top players` 统一使用更醒目的粗体，文字在日间模式为黑色、暗夜模式为白色。

### Verification

- 覆盖两类右侧榜单的明暗主题标题、图标比例及单行布局。

## [2.22.9] - 2026-08-19

### Changed

- `Top games` 的单色线框火焰改为彩色火焰，并将图标与标题字号等比例放大，提升紧凑榜单中的辨识度。

### Verification

- 保持标题单行、右侧全部游戏入口及 Popular/New 切换布局不变。

## [2.22.8] - 2026-08-19

### Changed

- 游戏榜和玩家榜标题区分别只保留 `Top games`、`Top players`，删除语义重复的 `Game Rankings`、`Arena Legends`，并收紧标题区与切换按钮的垂直间距。
- 保留游戏榜入口、玩家榜刷新按钮以及 Popular/New、Daily/All-time 切换，把释放出的高度用于显示更多榜单条目。

### Verification

- 覆盖 1366×768 等中等桌面高度下的两类右侧榜单标题区、操作按钮和榜单切换。

## [2.22.7] - 2026-08-19

### Fixed

- 桌面主游戏与右侧榜单的默认组合宽度由单纯的 `61.46vw` 调整为 `clamp(960px, 70vw, 1574px)`，并保留 16px 视口安全边距；1366×768 等中等桌面分辨率现在能够为 Popular/New 游戏榜保留游戏名称所需空间。
- 站点仍可通过既有主题字段覆盖桌面比例和最大宽度，不影响移动端单列布局，也不会拉宽正文、FAQ 与评论区。

### Verification

- 覆盖 1200px 桌面断点、1366×768 常见笔记本分辨率、1574px 最大宽度和窄视口安全边距。

## [2.22.6] - 2026-08-17

### Fixed

- 旧站提取器测试改为依据当前筛选配置动态验证每个生成属性，不再强制站点必须声明多人或双人模式。

### Verification

- 覆盖 Geometry Dash 等纯单人游戏站只提供 `single-player` 时，迁移提取器与完整构建仍能通过。

## [2.22.5] - 2026-08-17

### Fixed

- 旧站提取器测试改为验证通用的多人模式推断，不再要求每个站点的玩家模式筛选中必须提供 `two-player` 选项。

### Verification

- 覆盖 Hill Climb Racing 仅声明单人和多人模式时，迁移提取器与完整构建仍能通过。

## [2.22.4] - 2026-08-17

### Fixed

- 旧站提取器测试改为验证通用的双人模式推断，不再强制要求当前站点必须存在射击类 `pvp` 玩法标签。

### Verification

- 覆盖 Text Twist 等非射击站点采用独立玩法分类后，提取器仍能验证玩家模式推断且完整构建通过。

## [2.22.3] - 2026-08-17

### Fixed

- 本地可视化后台测试不再假设站点 Logo 必须是本地 WebP；改为验证当前站点 `public` 中任意真实图片，兼容 Logo 使用远程资源域名的迁移站点。

### Verification

- 覆盖 Text Twist 使用远程导航 Logo、同时保留本地 favicon 与封面素材的迁移场景。

## [2.22.2] - 2026-08-17

### Fixed

- 当目标竞赛适配器及迁移分组已经完全一致、生成计划无需重写迁移清单时，站点生成器测试改为读取现有 `competition-migrations.json`，正确验证真正的幂等状态。

### Verification

- 覆盖 Text Twist 已启用 `word-score` 且再次构建时零迁移写入的场景。

## [2.22.1] - 2026-08-17

### Fixed

- 站点生成器测试兼容已经安装目标竞赛适配器的网站：首次选择 `word-score` 时仍验证追加迁移，后续构建则验证既有迁移分组保持注册，不再错误要求重复创建同一迁移。

### Verification

- Text Twist 迁移站选择 `word-score` 后能够重复运行生成器与完整构建测试，迁移历史保持幂等。

## [2.22.0] - 2026-08-17

### Changed

- 纯模板默认关闭跑马灯、玩家排行榜和对局事件；首页仍保持 80/20 游戏区，并在玩家榜关闭时自动显示 Popular/New 游戏排行，零 D1、零玩家数据也能完整使用。
- Popular 排序优先使用接口返回的真实播放量与收藏量，接口暂无对应数据时回退到游戏目录中的初始 `plays` 与 `favorites`；New 继续只按 `siteAddedAt` 排序。

### Verification

- 站点生成器测试新增静态热门数据回退断言；首页两种右栏模式继续复用同一 `GameRankingPanel` 插槽。

## [2.21.0] - 2026-08-17

### Added

- 首页内容区侧栏复用 `SquareGameRecommendations`：左侧竖向广告下方最多展示 2 个方形推荐，右侧目录下方最多展示 4 个，并与游戏内页保持相同的排除、去重、角标及响应式规则。
- 首页方形推荐完全读取现有游戏目录，不新增动态接口、D1 查询或浏览器缓存负担。

### Site configuration

- 反馈插件继续由站点级 `features.feedback` 与 `feedbackProjectKey` 控制；模板保留通用能力，单站可以关闭而不分叉或删除模板核心。

## [2.20.3] - 2026-08-17

### Fixed

- 站点蓝图导出、Local Site Studio 和 Cloudflare 部署测试移除 `1v1-lol`、`1v1-lol.cc`、23 款游戏、固定 Logo 和固定正文等站点专属断言，改为读取当前站点主游戏、目录规模、品牌资源、内容、域名及适配器；模板复制为其他游戏站后不再因测试样例写死而构建失败。
- 未显式填写 Legal 内容的蓝图会在规范化阶段生成站点专属默认内容并写回可编辑蓝图，保证“蓝图 → 运行文件 → 蓝图”首次生成后即保持一致；零收藏等可省略字段按语义零值验证。
- 构建期评分刷新在新站 API 尚未部署、返回 404 等情况下继续保留现有快照，但不再强制中断仍有网络句柄的 Node 进程，避免 Windows 下触发 `UV_HANDLE_CLOSING` 底层断言。
- 站点生成器测试在临时目录自行创建示例资源，不再从当前网站 `public/` 复制 1v1 图片；新站可以彻底移除模板示例品牌资源而不破坏构建。

### Verification

- 在 BuildNow GG 独立迁移站点中验证蓝图导出、生成往返与当前状态判定，并运行完整生产构建。

## [2.20.2] - 2026-08-17

### Fixed

- 旧站提取器会读取常见 `IF-Cover`/`GameCover3` 组件中的背景图和 Logo 路径；首页主游戏自动补充时不再把方形 Logo 同时误用为大幅封面。

### Verification

- BuildNow GG 主游戏自动使用 `/buildnow-gg-bj.webp` 作为封面，并保留 `/buildnow-gg-logo.webp` 为 Logo；提取测试覆盖两类资源识别。

## [2.20.1] - 2026-08-17

### Changed

- 游戏属性推断与首页主游戏自动补充成为旧站迁移的默认已接受策略，不再产生待审核 warning；迁移状态只由真实缺失项或阻断项降级。
- PvP 推断补充 1v1、2v2、3v3、4v4 和 duel 语义，使旧目录中的对抗游戏更准确地进入 Gameplay 筛选。

### Verification

- BuildNow GG 重新提取后可直接达到 `ready`；自动补充 `buildnow-gg` 首页主游戏，并为 20 款游戏生成属性，迁移后仍可通过实际游玩手动校正。
- 自动测试、文档校验和模板发布一致性检查见本版本发布结果。

## [2.20.0] - 2026-08-17

### Added

- 新增 `site:extract-legacy` 只读旧站提取器，可从标准 `stimulation-clicker` 旧站读取域名、SEO、单一分类、游戏目录、内页正文、首页游戏地址、YouTube 和 FAQ，并生成可交给 `site:create` 的站点蓝图及迁移报告。
- 首页主游戏未进入旧目录时自动补成待审核主游戏；旧站随机或不可核验的播放量、评分、收藏与点赞全部归零，避免把伪数据带入新数据库。
- 新增四组自动测试，覆盖静态 TypeScript 字面量解析、可执行指标跳过、只读保证、非支持模板阻断、多分类阻断和首页主游戏补全。

### Safety

- 默认命令只打印预览；只有显式指定 `--output`/`--report` 才写报告文件，并且不会修改旧站、复制资源、执行 SQL 或部署。
- 资源、筛选属性、竞赛适配器和警告项保留人工审核；当前限制为单一分类且最多 100 款游戏。

### Verification

- BuildNow GG 真实试提取获得 20 款游戏、25 条旧路由、0 条未映射路由、0 个阻断项；主游戏正确识别为 `buildnow-gg`，`site:create` 只读预览成功并列出 5 个待补本地资源。
- `site:extract-legacy:test`、TypeScript、生产构建和模板发布校验见本版本发布结果。

## [2.19.3] - 2026-08-17

### Changed

- 将 Player Mode、Controls、Load Speed、Gameplay、Perspective 的单色线框图标替换为独立绘制的彩色卡通 SVG，并采用深色描边和轻投影，形成接近 TopGames 分类图标的视觉层级。
- 图标继续由筛选组的 `icon` 字段驱动，不增加图片资源、网络请求或外部图标依赖。

### Verification

- 已完成桌面与 390px 移动端浏览器检查：五组图标正常显示，标签与胶囊按钮未被挤压且没有横向溢出。彩色 SVG 使用固定高对比描边，可直接复用现有明暗主题容器；TypeScript、生产构建和模板发布校验见本版本发布结果。

## [2.19.2] - 2026-08-17

### Changed

- 撤销窄桌面单列方形推荐：`1440px` 以下恢复隐藏，避免单列卡片在侧栏中过大；`1440px` 起继续使用两列小卡片。
- 保留 `2.19.1` 增加的角标外伸和立体阴影效果。

### Verification

- 已完成 1366px 实机浏览器隐藏验证和 1440px 两列小卡片验证；TypeScript、生产构建与模板发布校验见本版本发布结果。

## [2.19.1] - 2026-08-17

### Changed

- 方形游戏卡片的角标移到图片框左上外侧，并增加向下投影，使 Hot、New、Top 呈现更明确的立体层级。
- 桌面窄侧栏在 `1200–1535px` 改为单列并只显示前 2 个推荐；`1536px` 起恢复两列及完整数量，兼顾可读尺寸与侧栏高度。

### Verification

- 已完成 TypeScript 类型检查，以及 1200px、1536px 实机浏览器布局验证：窄桌面为单列 2 项，宽桌面为双列完整数量，角标正常外伸且没有被图片裁切。
- 悬停与键盘聚焦规则继续由同一组件统一控制：角标隐藏、图片轻微放大、渐变遮罩和游戏名出现；生产构建与模板发布校验见本版本发布结果。

## [2.19.0] - 2026-08-17

### Added

- 新增可复用的方形游戏推荐组件，提供 Hot 红色、New 紫色和 Top 金色单一角标；悬停或键盘聚焦时角标淡出，底部渐变和游戏名同步进入。
- 游戏内页右侧目录下方接入最多 4 个相关推荐，左侧竖向广告下方接入最多 2 个推荐，并自动排除当前游戏、排行排除项和左右重复项。

### Performance

- 推荐内容直接复用静态游戏目录，不新增 Pages Functions、D1 或独立卡片数据请求；`1440px` 以下隐藏侧栏方形卡片，避免窄屏密度过高。

### Verification

- 浏览器确认左右推荐分别为 2 张和 4 张、两列正方形排布；1440px 以下自动隐藏且没有横向溢出。
- 日间与暗色卡片背景均已适配；悬停或键盘聚焦后角标透明度由 1 变为 0，遮罩与标题透明度由 0 变为 1，标题位移由 8px 回到 0。

## [2.18.1] - 2026-08-17

### Fixed

- Tailwind 内容扫描新增受保护的 `site/` 目录，使 `site/theme.ts` 中配置的标签胶囊背景色、文字色、描边色及暗色模式类全部进入最终 CSS；修复标签文字有颜色但背景透明的问题。

### Verification

- 浏览器确认 PvP 胶囊由透明恢复为红色浅底，Building 恢复为琥珀色浅底；日间与暗色类均由同一站点标签映射生成。

## [2.18.0] - 2026-08-17

### Added

- Local Site Studio 的站点 Logo、导航 Logo、favicon、首页背景、游戏 Logo 和封面新增即时图片预览。
- 新增只读 `public/` 图片选择器，支持路径搜索和缩略图浏览；首页长文与游戏内页正文新增可排序的结构化分区编辑器，并保留原始 HTML 入口。

### Safety

- 图片目录 API 继续受临时令牌和同源限制；预览路由只允许回环访问、受支持图片类型与 `public/` 内安全路径，并跳过符号链接。
- 本版本不上传、下载或复制资源，避免绕过站点资源来源与授权审核；保存仍经过预览、资源检查、确认、备份、校验和失败回滚。

### Verification

- 浏览器实测 4 个首页图片字段、2 个游戏图片字段、4 个首页正文分区和游戏长文编辑入口；桌面与 390px 移动宽度均无横向溢出，控制台无错误。

## [2.17.0] - 2026-08-17

### Added

- Local Site Studio 新增 Page Content 区域，可视化管理首页封面文案、相关推荐、长文 HTML、YouTube、FAQ、热门页和筛选页全部界面/SEO 文案。
- 新增 Legal Content 区域，覆盖五个固定法律页面及 About Us 的标题、段落、链接文案和更新时间开关，同时保持公开路径不可误改。
- 新增 Deployment 区域，管理 Cloudflare Pages、D1 标识与位置、可发现的竞赛适配器，以及逐游戏排行排除清单；API Token 明确保持在蓝图之外。

### Changed

- 后台从 6 个配置区扩展到 9 个，并继续让所有表单和 Advanced JSON 共用 `site/blueprint.json`。
- 竞赛适配器下拉列表由本地 `competition-packs` 自动发现，新增适配器包后无需改后台前端选项。

### Verification

- 浏览器实测 5 条 FAQ、5 个 Legal 页面、5 个 About Us 区块、2 个竞赛适配器与 23 个排行排除选项；专用热门页字段的只读预览仅影响预期文件。

## [2.16.0] - 2026-08-17

### Added

- 新增 Local Site Studio：运行 `site:admin` 即可在本机通过表单管理站点身份、SEO、资源、路由、分类、主题、功能、游戏目录、游戏属性和筛选架构。
- 新增 Advanced JSON 入口，在专用表单逐步扩展期间仍可编辑完整蓝图，不产生第二套配置。
- 新增本地后台测试，覆盖静态资源语法、临时令牌、API 鉴权、跨域阻断、只读预览和保存确认。

### Safety

- 后台只绑定 `127.0.0.1`，每次启动使用随机临时令牌；公开 Next.js 页面与生产构建不包含后台路由。
- 保存固定经过“预览 → 资源检查 → 站点 ID 确认 → 备份 → 全量校验 → 成功写入/失败回滚”，并拒绝覆盖磁盘上已被其他进程更新的蓝图。

### Changed

- 抽取统一站点验证模块，让命令行生成器与可视化后台复用完全相同的站点、筛选、后端和 TypeScript 验收流程。

## [2.15.0] - 2026-08-17

### Added

- 新增受保护的 `site/blueprint.json`，把站点身份、路由、主题、功能、游戏目录、完整正文、筛选、法律文案、竞赛与 Cloudflare 配置集中为可视化后台未来可直接读写的一份数据。
- 新增 `site:export`，可从现有站点运行文件反向导出蓝图；默认仅预览，应用前备份旧蓝图并使用临时文件安全替换。
- 新增 6 项蓝图导出测试，覆盖 TypeScript 字面量读取、完整内容保留、生成—导出无损往返、备份与当前状态检查。

### Changed

- `site:create` 默认读取 `site/blueprint.json`，不再要求每次指定 `--from`；应用后同时维护蓝图与运行文件。
- 站点生成器开始保留游戏初始播放、评分、票数、收藏、点赞、完整内页 HTML、分类短文案、筛选页全部界面文案和 Legal 内容。
- 评分快照在重新生成站点时保留已有游戏值，不再把全部游戏重置为零。

### Safety

- 蓝图反向导出使用 TypeScript AST 读取受控字面量，不执行站点模块；生成与反向导出必须通过哈希一致性测试后才可进入完整构建。

## [2.14.0] - 2026-08-17

### Added

- 新增 `cloudflare:provision`，支持预览并在精确确认后为每个新站创建独占 D1、取得 UUID 并写回站点配置。
- 新增 D1 位置提示配置，支持 Cloudflare 六个位置代码；面向美国东部的示例默认使用 `enam`。
- 部署测试增至 7 项，覆盖创建前临时配置、UUID 校验、配置备份写回和 Pages 配置式绑定。

### Changed

- Pages 部署改为从忽略的临时 Wrangler 工作区运行；工作区同时声明构建目录和 D1 绑定，部署后无需再去控制台重复绑定。
- 新站蓝图开始输出 Cloudflare 项目、D1 名称、绑定和位置，首次上线形成“审计 → 创建 → 建表 → 部署 → 健康检查”的固定流程。

### Safety

- 创建 D1 必须使用独立的 `<site/database/create>` 确认令牌；数据库 ID 已存在时拒绝再次创建。
- UUID 写回前备份 `site/cloudflare.json`，只接受 Wrangler 返回的预期绑定、数据库名和合法 UUID。

## [2.13.0] - 2026-08-17

### Changed

- Cloudflare 部署流程统一为“一站一套全新 D1”，移除旧数据库接管和 `manual-existing` 分支。
- `site:create` 不再生成迁移策略字段；部署审计、临时计划和站点校验只接受 Wrangler 管理的新数据库流程。
- 数据库文档移除 raw `d1 execute` 历史命令，明确首次从 `0001` 建表、后续只追加未执行结构升级。

### Safety

- 旧的 `database.migrationStrategy` 字段会被明确拒绝，避免新站误连旧库或绕过 Wrangler 迁移追踪。
- 远程迁移和 Pages 发布的预览、确认令牌及健康检查保护保持不变。

## [2.12.0] - 2026-08-17

### Added

- 新增 Cloudflare Pages + D1 部署向导，覆盖本地审计、临时配置、待执行迁移查询、远程迁移、Pages 发布和部署后健康检查。
- 新增受保护的 `site/cloudflare.json`，集中记录项目、域名、D1 绑定、迁移策略和健康探针，不保存 API Token。
- 新增 `/api/health` 轻量绑定探针，并用评分、日榜和跑马灯 GET 请求验证真实表结构。
- 新增 4 项部署测试，覆盖无凭证配置、绑定与迁移分组、临时工作区和只读健康检查。

### Safety

- 所有远程写入必须同时提供 `--apply` 和站点/目标组合确认令牌；默认命令只预览或只读。
- 临时 Wrangler 配置仅进入忽略的 `.wrangler`，并且只包含共享迁移和当前竞赛适配器迁移。
- 新站使用 Wrangler 原生迁移表；手工建库的旧站标记 `manual-existing` 并禁止自动重放历史 `ALTER TABLE`。
- 远程认证只读取 Wrangler 登录状态或 `CLOUDFLARE_API_TOKEN` 环境变量，站点配置不接受凭证字段。

## [2.11.0] - 2026-08-17

### Added

- 新增 `site:create`，通过单份 JSON 蓝图生成站点身份、主题、功能、路由、分类、筛选、首页、热门页、法律页基础内容、初始游戏目录、排行排除项和竞赛选择。
- 新增资源清单，明确区分本地、游戏资源域和远程资源；缺失本地资源只在预览中报告，应用前必须补齐。
- 新增 4 项生成器测试，覆盖完整规范化、干净目录与资源清单、重复执行幂等、验证失败全量回滚和竞赛迁移追加。

### Safety

- 新命令默认只读；应用前逐文件备份，并依次执行站点、筛选、后端和 TypeScript 校验。
- 生成器不下载或伪造游戏资源，不连接 D1、不执行 SQL、不部署，也不覆盖公共资源、Functions、已有迁移、远程广告配置或组件覆盖；未安装适配器只能追加包内不可变迁移及其分组。

### Architecture

- 动态分类路由改用稳定的 `PRIMARY_CATEGORY_PAGE` 契约，移除模板核心对 `oneVOneLolGames` 站点键名的依赖。
- `site:init` 保留为旧自动化兼容入口，新站创建流程统一迁移到 `site:create`。

## [2.10.0] - 2026-08-17

### Added

- 新增 `competition:install`，支持发现、列出、预览和安装内置竞赛适配器包。
- 新增 `1v1-lol` 与 `word-score` 包清单，统一适配器身份、指标、活动文案和迁移来源。
- 新增自动 D1 迁移编号、站点迁移分组、逐文件备份、验证失败回滚和重复安装幂等处理。
- 新增 4 项安装器测试，覆盖包发现、只读预览、成功安装、重复执行和失败恢复。

### Architecture

- `word-score` 成为模板内置可升级适配器；站点安装不再修改 backend 核心。
- 新站使用受保护的 `site/competition-migrations.json`，旧站继续兼容原 `backend/migrations.json`。
- 安装器只写站点保护区和追加迁移，不连接远程 D1，也不执行部署。

## [2.9.0] - 2026-08-17

### Added

- 新增跑马灯与排行榜跨游戏使用规范，统一胜局、积分、距离、生存时间和速通模型的稳定接口与交互边界。
- 规范游戏结算消息、跑马灯事件比例和文案色彩、排行榜响应、个人排名窗口、时区领奖台、缓存刷新、数据库查询与可信度要求。
- 新增新竞赛适配器验收清单，并把规范完整性接入文档机器校验。

### Governance

- AI 修改跑马灯、排行榜、档案、结算或竞赛适配器前必须阅读该规范。
- 游戏差异必须通过指标配置和适配器实现，禁止重新分叉通用竞赛 UI。

## [2.8.0] - 2026-08-17

### Added

- 新增 Text Twist 2 Untimed 文字积分制竞赛适配器样例，覆盖事件校验、批量上传、个人档案、跑马灯、日榜、总榜、个人窗口和主动刷新限制。
- 新增可复制 D1 表结构、游戏侧 `postMessage` 桥、示例事件和 `bestScore` 前端展示配置。
- 新增竞赛样例机器校验并接入本地开发和生产构建。

### Ranking

- Untimed 榜单按每位玩家单局最高分排列，不累计多局积分，也不使用完成时长。
- 平分时依次比较轮次、单词数、Bingo 数、最长词、更早达成时间和档案 ID。

### Safety

- 样例默认不启用，不影响当前 1v1 适配器、站点配置或 D1 数据库。
- 仅保存聚合数字，不保存猜词内容或题库文本；同档案 `runId` 唯一，重试不会重复记分。
- 明确标注客户端积分只适用于社区娱乐排行；奖品竞赛必须使用可信签名或服务端可验证计分。
- 目标站迁移继续位于保护区，启用时必须人工追加且不得回改已有迁移。

## [2.7.0] - 2026-08-16

### Added

- 新增受保护 Pages Functions 差异迁移向导，逐文件展示新增、替换、相同和目标站独有内容。
- 新增核心升级前置条件检查，避免新包装层引用尚不存在的后端模块。
- 新增逐文件备份、目标后端校验和失败自动恢复。
- 新增 3 项迁移测试，覆盖提前阻断、目标独有文件保留和回滚。

### Safety

- 默认只读；`--apply` 仍需明确指定。
- 目标站独有 Functions、D1 migrations、公共资源和环境变量不会被删除或覆盖。
- 迁移顺序固定为站点包接管、核心升级、Functions迁移、完整生产构建。

### Verified

- 对现有旧站只读识别出12条公开API路由、14个待替换文件、1个相同文件和5个保留的旧 `_shared` 文件；因前置核心尚未升级而正确阻断写入。

## [2.6.0] - 2026-08-16

### Added

- 新增旧网站接管向导，支持域名推断、路由盘点、旧配置识别、站点包核验和受保护 Functions 差异报告。
- 新增分阶段接管流程：先补齐受保护站点包并记录旧核心基线，再通过现有多站工具预览真正升级。
- 新增 3 项接管测试，覆盖身份识别、只读审计和跨域站点包阻断。

### Safety

- 默认只读并生成报告；必须明确使用 `--apply` 才写入。
- 接管阶段不替换旧核心、公共资源、Pages Functions、D1 迁移或环境变量。
- 域名不匹配、残缺站点包或已有基线会阻止写入；建立基线失败会回滚新增站点包。

### Verified

- 对现有 1v1-lol 旧站完成只读审计：识别 33 条路由、4 个旧配置文件、24 个站点包文件及 14 个受保护 Functions 差异，原站未被修改。

## [2.5.0] - 2026-08-16

### Added

- 新增本机多网站目标清单、示例配置和批量升级命令。
- 新增按网站筛选、批量预览、批量应用、首次接管和完整详情模式。
- 新增结构化升级报告，汇总每站版本、新增、更新、删除、冲突和失败原因。
- 新增 4 项多站工具测试，覆盖路径、重复配置、结果解析和总计汇总。

### Safety

- 默认仅预览；写入仍必须明确使用 `--apply`。
- 每个网站继续复用单站同步器的保护区、冲突阻断和独立备份机制。
- 单个网站失败不会中断其他网站检查；本机路径清单和报告不进入发布包。

## [2.4.0] - 2026-08-16

### Added

- 新增站点级 `site/game-filters.json`，统一声明筛选组、属性字段、选项、别名、默认值、图标和生成器参数。
- 新增 `filters:check`，同时校验属性配置和全部游戏目录数据。
- 新增非射击属性测试，确认同一生成器能够直接服务其他游戏类型。

### Changed

- 筛选页、Similar Games、排行标签和游戏属性读取改用统一站点属性配置。
- `game:add` 不再写死五组射击属性，按站点配置动态生成任意属性字段。
- 固定射击命名的核心文件更名为通用游戏筛选命名；现有 `/shooting-games` 路径和页面文案不变。

### Compatibility

- 当前 23 个游戏的筛选、排序、相似推荐和生成器参数保持兼容。
- `site/game-filters.json` 位于站点保护区，模板升级不会覆盖每个网站自己的属性体系。

## [2.3.0] - 2026-08-16

### Added

- 新增 `game:add` 游戏数据生成器，默认只预览，确认后才写入权威游戏目录。
- 支持单条参数与最多 100 条的 JSON 批量导入。
- 新增批量导入示例，支持分类、资源、平台、五组属性、YouTube 和对局桥配置。
- 新增 6 项生成器测试，覆盖默认值、类型化输出、重复 ID、属性校验、JSON 字段转换、备份和失败回滚。

### Safety

- 每次写入前在 `backups/game-add` 建立可恢复备份。
- 整批写入后自动执行站点校验；任何失败都会恢复原目录。
- 不生成虚假互动数据，新游戏固定从零播放量和全局评分基础权重开始。

### Changed

- 生成器测试接入生产构建，游戏目录插入标记纳入站点校验。

## [2.2.0] - 2026-08-16

### Added

- 新增可升级的 `backend` 核心层，包含通用 D1/HTTP 能力、社区模块和竞赛适配器契约。
- 新增 `1v1-lol` 竞赛适配器，承载对局、战绩、跑马灯、奖牌和排行榜快照逻辑。
- 新增 `site/backend.ts`，集中选择 D1 绑定名和竞赛适配器。
- 新增迁移分组清单、迁移计划命令和后端结构机器校验。

### Changed

- 12 个现有 Pages Functions 路由改为轻量兼容包装，公开 API 地址和响应契约保持不变。
- 评论、评分、互动和游戏卡片接口迁入通用社区模块。
- 10 个历史迁移在不移动、不改名、不修改内容的前提下划分为社区组和 1v1 适配器组。
- 后端校验接入本地开发和生产构建，`backend/` 纳入模板核心与发布哈希。

### Compatibility

- 现有 D1 无需重建或重新执行迁移。
- 前端、游戏 iframe、缓存键和 `/api/*` 请求地址无需修改。

## [2.1.0] - 2026-08-16

### Added

- 新增系统级 `TEMPLATE-GUIDE.md`，集中记录架构、配置、数据、缓存、部署、版本和 AI 修改规则。
- 新增模板更新记录制度与文档机器校验。
- 新增根目录 `AGENTS.md`，要求 AI 修改前阅读指南、修改后更新指南和 Changelog。

### Changed

- 文档版本与 `template/template.json` 版本绑定。
- 文档检查接入本地开发、生产构建与模板发布验证。

## [2.0.0] - 2026-08-16

### Added

- 将模板核心与 `site` 站点包物理分离。
- 建立统一站点 manifest、动态路由、游戏目录和构建期配置生成。
- 建立组件插槽与 `site/overrides` 覆盖层。
- 建立模板语义版本、核心 SHA-256 发布清单和多网站安全同步工具。
- 模板同步支持首次接管、只读预览、冲突识别、自动备份和保护区。

### Verified

- 23 个游戏内页、35 个静态页面、Sitemap 和 Pages Functions 完整构建通过。
