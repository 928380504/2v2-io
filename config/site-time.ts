/**
 * The single business-day boundary used by daily site features.
 * Change this IANA time-zone name when reusing the template for another site.
 */
import { SITE_CONFIG } from "@/config/site";

export const SITE_TIME_ZONE = SITE_CONFIG.timeZone;
